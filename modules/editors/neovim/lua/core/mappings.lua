-- Leader key
vim.g.mapleader = ","

---- Use semicolon to enter command mode also
--utils.map("n", ";", ":")
--
---- Move by visual line not actual line
--utils.map("n", "j", "gj")
--utils.map("n", "k", "gk")
--
---- Searching --
----
---- Map <Space> to / (search) and Ctrl-<Space> to ? (backwards search)
--utils.map("n", "<space>", "/")
--utils.map("n", "<c-space>", "?")

--utils.map('n', '<C-r>', '<cmd>lua reload_nvim_conf()<CR>')
